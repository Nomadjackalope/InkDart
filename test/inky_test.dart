import 'package:flutter_test/flutter_test.dart';

import 'package:inky/inky.dart';

void main() {

  // test('Reader readNumber -large, 0, 1, large', () {
    // final readerNegativeLarge = Reader("${-double.maxFinite.toInt()}");
    // expect(readerNegativeLarge.readNumber(), -double.maxFinite.toInt());

    // expect(Reader("{\"inkVersion\":20}"), 0);

    // final readerOne = Reader("1");
    // expect(readerOne.readNumber(), 1);

    // final readerLarge = Reader("${double.maxFinite.toInt()}");
    // expect(readerLarge.readNumber(), double.maxFinite.toInt());
  // });

  // test('Reader readArray', () {
  //   expect(Reader("[\"^Once upon a time...\",\"\\n\"]"),
  //       ["^Once upon a time...", "\\n"]);
  // });
}
